---
/* Wraps a band of the page in a palette. The nav recolours to match.
   Usage: <Section theme="sand">...</Section>
   Themes available: ink | sand | bone | clay  (defined in tokens.css) */
interface Props { theme?: 'ink' | 'sand' | 'bone' | 'clay'; full?: boolean; id?: string; class?: string; }
const { theme = 'ink', full = false, id, class: cls = '' } = Astro.props;
---
<section
  data-theme={theme}
  data-themeband
  id={id}
  class={`u-section ${full ? '' : 'u-shell'} ${cls}`}
  style="background: var(--bg); color: var(--fg);"
>
  <slot />
</section>
